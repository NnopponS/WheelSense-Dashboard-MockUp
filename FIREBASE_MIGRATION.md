# 🔥 Firebase Migration Guide - WheelSense Dashboard

## สรุปการเปลี่ยนแปลง (Summary)

ระบบ WheelSense Dashboard ได้ถูกอัพเกรดจาก **Local Storage** เป็น **Firebase Realtime Database** แล้ว ซึ่งจะทำให้:

### ✅ ประโยชน์ที่ได้รับ

1. **Cross-Device Synchronization (ซิงค์ข้ามเครื่อง)**
   - Demo Control และ User Dashboard สามารถทำงานร่วมกันได้แม้จะเปิดคนละเครื่อง
   - ไม่จำเป็นต้องใช้ browser เดียวกันและเปิด 2 tabs อีกต่อไป

2. **Real-time Updates (อัพเดทแบบ Real-time)**
   - เมื่อ Demo Control เปลี่ยน scene ระบบจะอัพเดท User Dashboard ทันที
   - อุปกรณ์ทั้งหมดจะ sync กันแบบ real-time

3. **Data Persistence (ข้อมูลจะไม่หาย)**
   - ข้อมูลทั้งหมดเก็บบน Firebase จะไม่หายแม้ปิด browser
   - สามารถเข้าถึงข้อมูลจากทุกอุปกรณ์ที่เชื่อมต่อ Firebase

---

## 📦 ข้อมูลที่ถูก Migrate ไป Firebase

### 1. **Building / Floor / Room Data (ข้อมูลอาคาร/ชั้น/ห้อง)**
   - Buildings (อาคาร): `B1 - Hospital`, `B2 - Smart Home`
   - Floors (ชั้น): `H-F1`, `H-F2`, `S-F1`
   - Rooms (ห้อง): ทุกห้องในแต่ละชั้น
   - Corridors (ทางเดิน): corridors ของแต่ละชั้น

### 2. **Patient Data (ข้อมูลผู้ป่วย)**
   - รายชื่อผู้ป่วยทั้งหมด
   - ข้อมูลการรักษา, ยา, แพทย์
   - สถานะผู้ป่วย (active, discharged, emergency)
   - การเพิ่ม/แก้ไข/ลบ sync แบบ real-time

### 3. **Device Data (ข้อมูลอุปกรณ์)**
   - Wheelchairs, Nodes, Gateways
   - Smart Home Appliances (ไฟ, พัดลม, แอร์, ประตู)
   - สถานะ on/off และค่าต่างๆ
   - อัพเดทแบบ real-time ข้ามทุกอุปกรณ์

### 4. **Demo State (สถานะการ Demo)**
   - สถานะปัจจุบันของ Demo mode
   - Step ที่กำลังทำงาน
   - ห้องที่ wheelchair อยู่
   - **ใช้ Firebase เป็นตัวกลางในการ sync ข้ามเครื่อง**

---

## 🔧 Technical Implementation

### ไฟล์ที่สร้างใหม่

1. **`src/lib/firebase.ts`**
   - Firebase configuration และ initialization
   - เชื่อมต่อกับ Firebase Realtime Database

2. **`src/lib/firebase-service.ts`**
   - Service layer สำหรับจัดการข้อมูลใน Firebase
   - CRUD operations สำหรับ Buildings, Floors, Rooms, Corridors
   - Patient management functions
   - Device management functions
   - Demo state synchronization
   - Real-time subscriptions (onValue listeners)

### ไฟล์ที่แก้ไข

1. **`src/lib/store.tsx`**
   - เปลี่ยนจาก `useLocalStorage` เป็น `useState` + Firebase
   - เพิ่ม Firebase initialization และ real-time subscriptions
   - Update functions ทั้งหมดใช้ Firebase แทน localStorage

2. **`src/components/demo-control-page.tsx`**
   - ลบ localStorage sync code
   - ใช้ Firebase ผ่าน store context
   - อัพเดท demo state ไป Firebase โดยตรง

3. **`src/components/patient-management-v2.tsx`**
   - เชื่อมต่อกับ Firebase service
   - Real-time patient data updates
   - CRUD operations ผ่าน Firebase

4. **`src/components/user-page.tsx`**
   - ลบ localStorage polling และ storage events
   - ใช้ `demoState` จาก store context ซึ่ง sync จาก Firebase
   - อัพเดท UI เมื่อ demoState เปลี่ยนแปลง

### Package ที่เพิ่ม

```json
{
  "firebase": "^11.0.2"
}
```

---

## 🚀 การใช้งาน

### วิธีการทดสอบ Cross-Device Synchronization

#### 1. **เปิด Demo Control (เครื่องที่ 1 หรือ Browser ที่ 1)**
```
http://localhost:5173/demo-control
```

#### 2. **เปิด User Dashboard (เครื่องที่ 2 หรือ Browser อื่น)**
```
http://localhost:5173/user
```

#### 3. **ทดสอบ Real-time Sync**
   1. คลิก Scene ต่างๆ ใน Demo Control
   2. สังเกตว่า User Dashboard จะอัพเดทแบบ real-time
   3. อุปกรณ์ (ไฟ, พัดลม, ประตู) จะเปลี่ยนสถานะตาม scene
   4. AI Assistant จะแสดงข้อความตาม scene

#### 4. **ทดสอบ Patient Management**
   1. เปิด Patient Management บนเครื่องที่ 1
   2. เพิ่ม/แก้ไข/ลบผู้ป่วย
   3. เปิด Patient Management บนเครื่องที่ 2
   4. ข้อมูลจะอัพเดทแบบ real-time

---

## 🔐 Firebase Configuration

Firebase URL ที่ใช้:
```
https://wheelsense-201ae-default-rtdb.firebaseio.com/
```

### โครงสร้างข้อมูลใน Firebase

```
wheelsense-201ae-default-rtdb/
├── buildings/          # อาคาร
├── floors/             # ชั้น
├── rooms/              # ห้อง
├── corridors/          # ทางเดิน
├── devices/            # อุปกรณ์ทั้งหมด
├── meshRoutes/         # เส้นทาง mesh network
├── patients/           # ข้อมูลผู้ป่วย
├── demoState/          # สถานะ demo (real-time sync)
└── wheelchairPositions/ # ตำแหน่ง wheelchair (real-time)
```

---

## 🧪 การทดสอบ

### Test Cases ที่ควรทดสอบ

#### 1. **Demo Control ↔ User Dashboard Sync**
- [ ] เปิด Demo Control และ User Dashboard บนเครื่องต่างกัน
- [ ] คลิก scene ใน Demo Control
- [ ] ตรวจสอบว่า User Dashboard อัพเดท room และ AI messages
- [ ] ตรวจสอบว่าอุปกรณ์ (ไฟ, พัดลม, ประตู) เปลี่ยนสถานะ

#### 2. **Patient Management Sync**
- [ ] เปิด Patient Management บน 2 เครื่อง
- [ ] เพิ่มผู้ป่วยใหม่บนเครื่องที่ 1
- [ ] ตรวจสอบว่าเครื่องที่ 2 เห็นผู้ป่วยใหม่ทันที
- [ ] แก้ไขข้อมูลบนเครื่องที่ 2
- [ ] ตรวจสอบว่าเครื่องที่ 1 เห็นการเปลี่ยนแปลง

#### 3. **Device Control Sync**
- [ ] เปิด User Dashboard บน 2 เครื่อง
- [ ] เปิด/ปิดอุปกรณ์บนเครื่องที่ 1
- [ ] ตรวจสอบว่าเครื่องที่ 2 เห็นการเปลี่ยนแปลง

#### 4. **Map Editor Sync**
- [ ] เปิด Map Editor บน 2 เครื่อง
- [ ] สร้าง/แก้ไขห้องบนเครื่องที่ 1
- [ ] ตรวจสอบว่าเครื่องที่ 2 เห็นการเปลี่ยนแปลง

---

## 🐛 Troubleshooting

### ปัญหา: ข้อมูลไม่ sync
**วิธีแก้:**
1. ตรวจสอบ console ว่ามี error จาก Firebase หรือไม่
2. ตรวจสอบว่า Firebase URL ถูกต้อง
3. ลอง refresh browser ทั้ง 2 เครื่อง

### ปัญหา: Firebase connection error
**วิธีแก้:**
1. ตรวจสอบ internet connection
2. ตรวจสอบว่า Firebase Database Rules อนุญาตให้อ่าน/เขียนได้
3. ตรวจสอบ Firebase console ว่า database ทำงานปกติ

### ปัญหา: Demo state ไม่ sync ข้ามเครื่อง
**วิธีแก้:**
1. เปิด console บน Demo Control ตรวจสอบว่ามี log "🔥 Demo state saved to Firebase"
2. เปิด console บน User Dashboard ตรวจสอบว่ามี log "🔥 Firebase Sync"
3. ตรวจสอบ Firebase console ว่ามีข้อมูลใน `demoState` path

---

## 📊 Performance Notes

- **Latency:** Real-time sync ใช้เวลาประมาณ 100-300ms (ขึ้นอยู่กับ network)
- **Bandwidth:** Firebase optimizes data transfer โดยส่งแค่ส่วนที่เปลี่ยนแปลง
- **Offline Support:** Firebase มี offline cache แต่ต้อง configure เพิ่มเติม

---

## 🔄 Migration from Old System

ระบบเก่าใช้:
- `localStorage` + `storage events` สำหรับ cross-tab sync
- จำกัดอยู่ที่ browser เดียวกัน
- ต้องเปิด 2 tabs ในเครื่องเดียวกัน

ระบบใหม่ใช้:
- Firebase Realtime Database
- Real-time sync ข้ามเครื่อง, ข้าม browser
- เปิดคนละเครื่องก็ sync กันได้

---

## 📝 Notes

1. **Data Initialization:** 
   - ครั้งแรกที่รัน Firebase จะถูก initialize ด้วย default data
   - หากต้องการ reset ข้อมูล ให้ใช้ฟังก์ชัน `resetDemoData()` ใน Settings

2. **Firebase Rules:**
   - ตอนนี้ Firebase อาจยัง open สำหรับทดสอบ
   - Production ต้อง configure authentication และ rules

3. **Future Improvements:**
   - Authentication (ให้ user login ก่อนใช้งาน)
   - Offline support (cache data when offline)
   - History/Audit logs (บันทึกการเปลี่ยนแปลงทั้งหมด)

---

## 🎉 สรุป

ระบบได้ถูก migrate เป็น Firebase สำเร็จแล้ว! ตอนนี้คุณสามารถ:

✅ เปิด Demo Control และ User Dashboard บนคนละเครื่อง  
✅ Sync แบบ real-time ข้ามอุปกรณ์ทั้งหมด  
✅ จัดการผู้ป่วย, อุปกรณ์, และแผนที่จากที่ไหนก็ได้  
✅ ข้อมูลจะไม่หายแม้ปิด browser  

**ทดสอบได้เลยครับ! 🚀**

